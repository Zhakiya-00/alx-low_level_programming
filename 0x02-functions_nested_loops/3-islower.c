#include "main.h"

/*
 * islower - check if c is lowercase
 *
 * Return 1 - if c is lowercase
 *
 * Return 0 - if otherwise
 */

int _islower(int c)


	{

		if ( _islower(c) )

		{
		return (1);

	}
	else
	{
		return (0);

	}
}
