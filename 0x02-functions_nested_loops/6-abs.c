#include "main.h"

/**
 * _abs - returns absl value n of an int
 * @a: parse me
 *
 * Return : the absolute value of int
 */

int _abs(int a)
{
	if (a < 0)
	{
		return(-a);
	}
	else
	{
		return (a);
	}
}

